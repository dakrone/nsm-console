test -e ${ARG3_FLOW_FILE} || argus ${ARGUS_OPTIONS} -r ${PCAP_FILE} -w ${ARG3_FLOW_FILE} ${ARGUS_FILTER}
test -e ${ARG3_SESS_FILE} || racluster ${RACLUSTER_OPTIONS} -r ${ARG3_FLOW_FILE} -w ${ARG3_SESS_FILE} ${RACLUSTER_FILTER}
